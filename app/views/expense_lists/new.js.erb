var form = "<%= j render 'form' %>";
var expenseLists = $('#expense-lists');
var createButton = $('.create-expense-list-button');

expenseLists.append(form).fadeIn(200);
createButton.hide();

form_field.on('click', '.back-link', function(){
  form_field.slideUp(200);
  createButton.show();
});
