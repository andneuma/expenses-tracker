var form_field = $('#expense_list_form_panel');
var expenseLists = $('#expense-lists');
var createButton = $('.create-expense-list-button');

form_field.remove();
expenseLists.append("<%= j render @expense_list %>");
createButton.show(200);
